------------------
Application Notes:
------------------
(1) TBA

------
TO DO:
------
In no particular order.

(1) The repository does both the database and the network service.
    - Change needed to accommodate for retrofit (ref Android std architecture).

(2) Filter the listing showing in the main recyclerview.
    - When app starts, use preference value if set.
        - Else default is toggle all on ?
    - Dependant on which toggle button selected.
        - This only filters what is coming from the cache.
        - Use object's 'meetingType'.

(3) Preferences.
    - A default meeting type, i.e. an initial filter on the main recyclerview.
    - Control the availability of the Delete (All) menu option.
    - Use cache if exists, rather than a network download.
        - Data age, i.e. no point having yesterday's.

(4) Display list of Races?
    - Dependant on what meeting is selected.
        - Will need new:
            - entities
            - storage
            - linkage

(5) TBA


